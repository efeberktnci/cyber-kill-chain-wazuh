# Detection Content

This directory will contain version-controlled Wazuh rules, Sysmon configuration references, queries, and future Sigma content. Detection files must state their purpose, prerequisites, test evidence, expected false positives, and rollback considerations.

